# คำแนะนำการตั้งค่าระบบ Payroll System

## การตั้งค่า Supabase

### ขั้นตอนที่ 1: สร้าง Supabase Project

1. ไปที่ [Supabase](https://supabase.com)
2. คลิก "Start your project" หรือ "New project"
3. เลือก Organization หรือสร้างใหม่
4. กรอกข้อมูล Project:
   - **Name:** payroll-system (ตามใจชอบ)
   - **Database Password:** สร้างรหัสผ่านที่แข็งแรง (เก็บไว้)
   - **Region:** Southeast Asia (Singapore) - ใกล้ไทยที่สุด
5. คลิก "Create new project"
6. รอ 2-3 นาทีให้ Project สร้างเสร็จ

### ขั้นตอนที่ 2: นำ API Keys มาใช้งาน

1. ใน Supabase Dashboard ไปที่ **Settings** > **API**
2. คัดลอกค่าต่อไปนี้:
   - **Project URL:** `https://xxx.supabase.co`
   - **anon public key:** `eyJxxx...` (API key ยาวๆ)

3. เปิดไฟล์ `.env.local` ในโปรเจค
4. แก้ไขค่าให้ตรงกับที่คัดลอกมา:

```env
NEXT_PUBLIC_SUPABASE_URL=https://ooipmyvpvpdbffjvbcej.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxx...
```

### ขั้นตอนที่ 3: สร้าง Database Tables

1. ใน Supabase Dashboard ไปที่ **SQL Editor**
2. คลิก "+ New query"
3. คัดลอกเนื้อหาทั้งหมดจากไฟล์ `/Users/piw/Downloads/dem/database_schema.sql`
4. วางใน SQL Editor
5. คลิก "Run" หรือกด Ctrl+Enter
6. รอจนกว่า SQL จะ run เสร็จ (ประมาณ 5-10 วินาที)

**ตรวจสอบว่าสร้างสำเร็จ:**
- ไปที่ **Table Editor**
- ควรเห็น Tables ต่อไปนี้:
  - employees
  - attendance_scans
  - daily_attendance
  - leave_records
  - payroll_periods
  - payroll_calculations
  - tax_settings
  - social_security_settings

### ขั้นตอนที่ 4: ตั้งค่า Row Level Security (RLS) - Optional

สำหรับระบบที่ต้องการความปลอดภัย:

```sql
-- ปิด RLS สำหรับการใช้งานครั้งแรก (ง่ายที่สุด)
ALTER TABLE employees DISABLE ROW LEVEL SECURITY;
ALTER TABLE attendance_scans DISABLE ROW LEVEL SECURITY;
ALTER TABLE daily_attendance DISABLE ROW LEVEL SECURITY;
ALTER TABLE leave_records DISABLE ROW LEVEL SECURITY;
ALTER TABLE payroll_periods DISABLE ROW LEVEL SECURITY;
ALTER TABLE payroll_calculations DISABLE ROW LEVEL SECURITY;

-- หรือเปิด RLS และกำหนด Policy (สำหรับ Production)
-- ต้องมีระบบ Authentication ก่อน
```

**หมายเหตุ:** สำหรับระบบที่ใช้งานจริง แนะนำให้เปิด RLS และตั้งค่า Authentication

## การตั้งค่า Local Development

### ขั้นตอนที่ 1: Clone หรือย้ายโปรเจค

```bash
cd /Users/piw/Downloads/dem/payroll-system
```

### ขั้นตอนที่ 2: ติดตั้ง Dependencies

```bash
npm install
```

### ขั้นตอนที่ 3: ตั้งค่า Environment

แก้ไขไฟล์ `.env.local` ตามที่ได้จาก Supabase:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

### ขั้นตอนที่ 4: Run Development Server

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ [http://localhost:3000](http://localhost:3000)

## การนำเข้าข้อมูลเริ่มต้น

### นำเข้าข้อมูลพนักงาน

1. เตรียมไฟล์ข้อมูลพนักงาน (Tab-separated):

**ไฟล์: employees.txt**
```
No.	Name	Department	CertificateType
20055885	เสาวลักษณ์ ชาวโคกหม้อ	Assembly	Other
20055547	เสาวณีย์ ชอบชื่น	Assembly	Other
20055559	เพ็ญศิริ ลำจวน	Assembly	Other
20055054	สุวรรณี ไกรษี	Engineer	Other
```

2. เปิดระบบที่ [http://localhost:3000](http://localhost:3000)
3. ไปที่หน้า **"พนักงาน"**
4. คลิก **"นำเข้า CSV"**
5. เลือกไฟล์ employees.txt

### นำเข้าข้อมูลการแสกน (ทดสอบ)

**ไฟล์: attendance_sample.txt**
```
01	11-10-2025	00:27:19	'20056402		1
01	11-10-2025	00:37:30	'20056415		1
01	11-10-2025	00:52:56	'20056330		1
04	11-10-2025	20:13:39	'20055675		2
04	11-10-2025	21:05:25	'20056298		2
```

1. ไปที่หน้า **"บันทึกเวลา"**
2. วางข้อมูลข้างต้นในช่อง text area
3. คลิก **"นำเข้าข้อมูล"**

## การแก้ไขปัญหาที่พบบ่อย

### 1. ไม่สามารถเชื่อมต่อ Supabase

**Error:** "Failed to fetch" หรือ "Network error"

**แก้ไข:**
- ตรวจสอบว่า SUPABASE_URL และ SUPABASE_ANON_KEY ถูกต้อง
- ตรวจสอบว่าไฟล์ `.env.local` อยู่ในโฟลเดอร์ root
- Restart dev server (`Ctrl+C` แล้ว `npm run dev` ใหม่)
- ตรวจสอบ internet connection

### 2. Tables ไม่ถูกสร้าง

**Error:** "relation does not exist"

**แก้ไข:**
- ไปที่ Supabase Dashboard > SQL Editor
- Run SQL จาก database_schema.sql ใหม่
- ตรวจสอบว่าไม่มี error ใน SQL execution

### 3. ข้อมูลพนักงานเข้าไม่ได้

**Error:** "Duplicate key value" หรือ "Failed to insert"

**แก้ไข:**
- ตรวจสอบว่ารหัสพนักงานไม่ซ้ำ
- ตรวจสอบรูปแบบไฟล์ (ต้องเป็น Tab-separated)
- ลองเพิ่มทีละคนก่อน

### 4. OT คำนวณผิด

**แก้ไข:**
- ตรวจสอบว่าข้อมูลพนักงานมี department ที่ถูกต้อง
- ตรวจสอบเวลาในข้อมูลการแสกน
- ตรวจสอบว่าวันที่เป็นวันอาทิตย์หรือไม่

### 5. หน้าเว็บ Error / Blank

**แก้ไข:**
1. เปิด Developer Console (F12)
2. ดู error messages
3. ตรวจสอบว่า imports ครบถ้วน
4. ตรวจสอบว่า API routes ทำงาน

**ทดสอบ API:**
```bash
curl http://localhost:3000/api/employees
```

## ขั้นตอนถัดไป

หลังจากตั้งค่าเสร็จแล้ว:

1. ✅ ทดสอบนำเข้าข้อมูลพนักงาน
2. ✅ ทดสอบนำเข้าข้อมูลการแสกน
3. ✅ ตรวจสอบการคำนวณ OT
4. ✅ ทดสอบบันทึกการลา
5. ✅ ทดสอบคำนวณเงินเดือน
6. 🚀 Deploy to Production (Vercel)

## Deploy to Production

### Deploy บน Vercel (แนะนำ)

1. Push โค้ดขึ้น GitHub:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/payroll-system.git
git push -u origin main
```

2. ไปที่ [Vercel](https://vercel.com)
3. คลิก "Add New Project"
4. Import GitHub repository
5. เพิ่ม Environment Variables:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
6. คลิก "Deploy"

### หลัง Deploy

1. ทดสอบระบบบน Production URL
2. ตั้งค่า Custom Domain (ถ้าต้องการ)
3. เปิดใช้งาน HTTPS (Vercel ทำให้อัตโนมัติ)
4. แจ้ง URL ให้ผู้ใช้งาน

## การบำรุงรักษา

### Backup ข้อมูล

1. ใน Supabase Dashboard ไปที่ **Database** > **Backups**
2. Supabase จะ backup อัตโนมัติทุกวัน
3. สามารถ restore ได้ตลอดเวลา

### ตรวจสอบ Usage

1. ใน Supabase Dashboard ไปที่ **Usage**
2. ตรวจสอบ:
   - Database size
   - API requests
   - Storage used

**Free tier limits:**
- Database: 500 MB
- API requests: 500,000/month
- Storage: 1 GB

### การอัพเดทระบบ

```bash
# อัพเดท dependencies
npm update

# ตรวจสอบ security vulnerabilities
npm audit

# แก้ไข vulnerabilities
npm audit fix
```

## ความช่วยเหลือเพิ่มเติม

- [Supabase Documentation](https://supabase.com/docs)
- [Next.js Documentation](https://nextjs.org/docs)
- [Vercel Deployment](https://vercel.com/docs)

---

สำเร็จแล้ว! ระบบพร้อมใช้งาน 🎉
